<Window x:Class="RogueViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RogueViewer"
        mc:Ignorable="d"
        Title="Rogue Viewer" Height="450" Width="800" Margin="10">
    <Grid>


        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <ListBox Grid.Column="0"
         ItemsSource="{Binding Instances}"
         SelectedItem="{Binding SelectedInstance}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding ProcessId}" FontWeight="Bold"/>
                        <TextBlock Text="{Binding Level}" FontSize="11"/>
                        <TextBlock Text="{Binding Runtime}" FontSize="11"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <TextBox Grid.Column="1"
             Text="{Binding SelectedInstance.MapText}"
             FontFamily="Consolas"
             FontSize="14"
             IsReadOnly="True"
             AcceptsReturn="True"
             VerticalScrollBarVisibility="Auto"
             HorizontalScrollBarVisibility="Auto"/>
    </Grid>
</Window>
